---
title: "example-workflow"
---

```{r, include = FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>"
)
```

```{r setup}
# library(protoclim)
```


Start by specifying where the data lives...

```{r}

```

Then run add QC codes 

```{r}
# add_quality_control_flags()
```


There's a whole world of ways to check for quality control issues. One way like is 

coordinatecleaner

Then run add covariates 

```{r}
# add_covariates()
```

Then run `calculate_covariate_area()`


```{r}
# calculate_covariate_area()
```


Then run `calculate_climate_suitability()`

```{r}
# calculate_climate_suitability()
```